export enum USER_ROLES {
    ADMIN = 'admin',
    
    FIGHTER = 'fighter',
    EventManager = 'ventManager',
}
  
  export enum status {
    active = 'active',
    delete = 'delete',
  }
  
  export enum AdminTypes {
    ADMIN = 'admin',
     
  }
  